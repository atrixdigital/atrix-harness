{
  "name": "atrix-harness",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "The Atrix agent operating system — rules, skills, graphs and orchestration for any coding agent.",
  "workspaces": ["packages/*"],
  "scripts": {
    "atrix": "bun run packages/cli/src/index.ts",
    "build": "bun run packages/cli/src/index.ts build",
    "typecheck": "tsc --noEmit",
    "test": "bun test",
    "lint": "eslint packages --ext .ts"
  },
  "engines": { "bun": ">=1.1.0" },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.6.0"
  },
  "dependencies": {
    "zod": "^3.23.8"
  }
}
