node_modules/
dist/
*.tsbuildinfo

# graph indexes — always local, never committed
.atrix/
*.db
*.db-wal
*.db-shm

# env & secrets
.env
.env.*
!.env.example

.DS_Store
*.log

# eval scratch
cross-tenant-probe.ts

# Projects are independent git repos cloned into the workspace. They keep their own
# remotes and history; client code never merges into the shared harness repo.
projects/
!projects/.gitkeep
